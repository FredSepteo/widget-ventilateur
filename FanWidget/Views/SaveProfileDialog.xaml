<Window x:Class="FanWidget.Views.SaveProfileDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Enregistrer le profil"
        Width="340"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#0F1419"
        FontFamily="Segoe UI Variable Text, Segoe UI">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Nom du profil" FontSize="15" FontWeight="SemiBold"
                   Foreground="{StaticResource TextBrush}" Margin="0,0,0,10"/>
        <TextBox x:Name="NameBox" Grid.Row="1" Style="{StaticResource AirflowTextBox}"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Annuler" Style="{StaticResource AirflowButton}" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Enregistrer" Style="{StaticResource AirflowPrimaryButton}" IsDefault="True" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
