<Window x:Class="FanWidget.Views.ProfileEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Modifier le profil"
        Width="480"
        Height="560"
        MinWidth="420"
        MinHeight="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="#0F1419"
        FontFamily="Segoe UI Variable Text, Segoe UI">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Profil utilisateur" FontSize="16" FontWeight="SemiBold"
                   Foreground="{StaticResource TextBrush}" Margin="0,0,0,12"/>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <TextBlock Text="Nom" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center"/>
            <TextBox x:Name="NameBox" Style="{StaticResource AirflowTextBox}"
                     HorizontalAlignment="Right" Width="280"/>
        </Grid>

        <CheckBox x:Name="P100CheckBox" Grid.Row="2" Style="{StaticResource AirflowCheckBox}"
                  Content="GPU P100 activé" Margin="0,0,0,12"/>

        <Border Grid.Row="3" Background="{StaticResource PanelBrush}" CornerRadius="10"
                BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="1"
                Padding="8" Margin="0,0,0,12">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="FanSettingsPanel"/>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Annuler" Style="{StaticResource AirflowButton}" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Enregistrer" Style="{StaticResource AirflowPrimaryButton}" IsDefault="True" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
